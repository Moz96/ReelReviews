<%= form_with(model: [@post, Comment.new], url: place_post_comments_path(@post), local: true) do |form| %>
  <%= form.text_area :content, placeholder: 'Write a comment...', rows: 2 %>
  <%= form.submit 'Post Comment' %>
<% end %>
