<style>
  #videoContainer {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: black;
    z-index: -1;
  }

  #videoElement {
    /* Need to update this to take footer into account */
    min-height: 100dvh;
  }

  .record-video-buttons {
  position: fixed;
  bottom: 50px;
  left: 0;
  right: 0;
  margin: 0 auto;
  text-align: center;
  z-index: 1;
  }

  .record-video-buttons .record-button {
  background-color: rgba(255, 0, 0, 0.5);
  border: none;
  padding: 10px 20px;
  font-size: 16px;
  border-radius: 50%;
  cursor: pointer;
  }

  .switch-camera-button {
  position: fixed;
  bottom: 50px;
  right: 20px;
  z-index: 1;
    }

  .switch-camera-button button {
  background-color: transparent;
  border: none;
  padding: 0;
  font-size: 30px;
  cursor: pointer;
  }

  .switch-camera-button button i {
  color: white;
  }
</style>
<div id="videoContainer">
  <video id="videoElement" data-record-video-target="videoElement" autoplay muted></video>
</div>

<div class="record-video-buttons">
  <button class="record-button" data-action="click->record-video#toggleRecording" data-target="record-video.recordButton">
    <i class="fas fa-circle"></i> <!-- Font Awesome recording icon -->
  </button>
</div>

<div class="switch-camera-button">
  <button data-action="click->record-video#toggleFlag" data-target="record-video.videoElement">
    <i class="fas fa-sync-alt"></i> <!-- Font Awesome switch camera icon -->
  </button>
</div>
